name: Test docker task

on:
  workflow_call:
  workflow_dispatch:

jobs:

  build-docker:
    name: Build docker images without pushing
    uses: ./.github/workflows/build-docker-task.yml
    secrets: inherit
    with:
      push: false
