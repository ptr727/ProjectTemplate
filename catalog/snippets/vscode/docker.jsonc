// Docker additions, merged into the base fragment for a repo that ships a Docker image.
// The Docker extension adds authoring support only, with no settings of its own.
{
    "extensions": {
        "recommendations": [
            "ms-azuretools.vscode-docker"
        ]
    }
}
